/* import React from "react";

STATUS FILTER:
const FilterStatus = (props) => {
  return (
    <>
      <label className="header-label" htmlFor="status">
        Pick a status!
      </label>
      <input
        className="header-inputStatus"
        type="text"
        name="status"
        id="status"
        placeholder=""
        /* onChange={this.handlerFilter} 
      />
    </>
  );
};

export default FilterStatus;
 */
